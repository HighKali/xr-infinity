# 🚀 XR∞Coin — Nodo orbitante, firma immortale

**XR∞Coin** è una criptovaluta industriale basata su Cryptonote, progettata per essere *etica, decentralizzata e narrativamente completa*.  
Ogni fork è vivo, ogni nodo è un battito, ogni firma è una stella nella costellazione XR∞.

---

## 🌌 Visione

XR∞Coin è il cuore pulsante della **XR∞ Industrial Constellation**, un ecosistema dove:

- Ogni nodo è validatore
- Ogni impresa è una costellazione
- Ogni transazione è un atto di memoria e governance

---

## 🔧 Caratteristiche tecniche

- 🔒 Protocollo: Cryptonote (derivato da Monero/Aeon)
- 🧬 Privacy: Ring signatures, stealth addresses
- ⚙️ Algoritmo PoW: RandomX ottimizzato
- 🛠️ Compilazione: CMake + Makefile
- 🌐 Compatibilità: Linux, macOS, Windows, Android (via Termux/Docker)
- 📦 Modularità: Submodule e build distribuita

---

## 📦 Requisiti per la compilazione

```bash
sudo apt update && sudo apt install \
  build-essential cmake git pkg-config \
  libboost-all-dev libssl-dev libzmq3-dev \
  libunbound-dev libsodium-dev libreadline-dev \
  libpgm-dev libnorm-dev libexpat1-dev doxygen graphviz
